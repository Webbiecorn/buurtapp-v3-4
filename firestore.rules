rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tijdelijk permissief voor ontwikkeling/productievalidatie
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
